# hydra
HYDRA — асинхронное ядро OSINT-разведки. Один таргет → 200+ источников (username / email / phone / ФИО / domain) → корреляция совпадений → 6 форматов отчёта. Passive по умолчанию, активные техники за явными флагами. Python 3.11+. OPSEC-first.
